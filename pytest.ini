[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    external_api: tests that require external API access (may be slow or skip if API unavailable)
    slow: tests that make live network calls (skip with -m "not slow")
